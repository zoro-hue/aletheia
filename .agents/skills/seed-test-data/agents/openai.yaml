interface:
  display_name: "Seed Test Data"
  short_description: "One-command Aletheia test data seeding"
  default_prompt: "Use $seed-test-data to seed the local Aletheia data this task needs (complex traces, long sessions, bulk traces, v4 events)."
